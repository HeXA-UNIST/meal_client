<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 운영시간 경계(운영 시작·마감임박)에 정확히 맞춰 위젯을 갱신하기 위함.
         권한이 없거나 사용자가 거부해도 setAndAllowWhileIdle로 대체 동작함(덜 정확할 뿐 크래시 없음). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="false"
        android:localeConfig="@xml/locales_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- 단일 통합 위젯 (역사적 이유로 클래스명은 2x2 유지 — 기존 인스턴스 보존) -->
        <receiver
            android:name=".BapUWidget2x2Provider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_2x2" />
        </receiver>

        <!-- 운영시간 경계(운영 시작·마감임박)마다 위젯을 갱신하는 예약 호출 수신자 -->
        <receiver
            android:name=".BapUWidgetScheduledUpdateReceiver"
            android:exported="false" />

        <!-- 재부팅 후 예약 호출 재등록 -->
        <receiver
            android:name=".BapUWidgetBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 예약 식단 알림 전달.
             ScheduledNotificationBootReceiver는 의도적으로 등록하지 않는다.
             flutter_local_notifications 22.3.0은 재부팅 시 저장된 one-shot의 과거 epoch도
             그대로 재예약하므로 AlarmManager가 오래된 식단 알림을 즉시 몰아서 보낼 수 있다.
             재부팅 뒤 복구는 앱 launch/resume의 미래 알림 재조정에 맡기며,
             사용자가 앱을 다시 열기 전에는 식단 알림을 보내지 않는다. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <!-- 식당 선택 설정 Activity -->
        <activity
            android:name=".BapUWidgetSingleConfigActivity"
            android:exported="true"
            android:theme="@style/WidgetConfigTheme">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
